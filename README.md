# Planet

CMAKE is ready.
the command to build :
cmake -S . -B build4
cmake --build build4 --config Release -- /m

It's supper slow cause it will build the thirdparty header from source
effect:
![planet](display.png)
